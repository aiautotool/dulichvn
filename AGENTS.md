# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Auto Build Directive
Tự động chạy rebuild ứng dụng lên máy thật Kct (`IOS_DEVICE="Kct" ./scripts/build-install-ios.sh`) sau khi hoàn tất chỉnh sửa code.

